<UserControl x:Class="modernWPF.Pages.radevutakip"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="900">
    <Grid Style="{StaticResource ContentRoot}">
        <ScrollViewer>
            <StackPanel MinWidth="200">
                <TextBlock Text="RANDEVU TAKİP" Style="{StaticResource Heading2}" />
                <TextBlock Text="RANDEVULARIM" />
                <DataGrid Name="dg1"></DataGrid>
                <Button Click="Button_Click_1" Margin="10">İPTAL ET</Button>
                <Button Margin="10" Click="Button_Click">çıkış</Button>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
